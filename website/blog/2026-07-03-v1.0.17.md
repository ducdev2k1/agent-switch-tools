---
slug: v1.0.17
title: Agent Switch Tools v1.0.17
authors: [ducdev2k1]
tags: [release, macos, keychain, claude, accounts]
date: 2026-07-03
---

**Agent Switch Tools v1.0.17** sửa một vấn đề nền tảng trên **macOS**: từ Claude Code 2.x, credentials của Claude Code được lưu trong **macOS Keychain** chứ không còn ở file `~/.claude/.credentials.json`. Các bản trước chỉ đọc/ghi file nên trên macOS việc chuyển tài khoản không thực sự có hiệu lực. Bản này đưa toàn bộ luồng credentials của tài khoản active đi qua Keychain trên macOS, còn Linux/Windows giữ nguyên như cũ.

{/* truncate */}

## Có gì mới?

### 1. Hỗ trợ macOS Keychain cho tài khoản active

Trên macOS, app giờ đọc và ghi credentials của tài khoản **đang active** trực tiếp trong login Keychain — đúng nơi Claude Code CLI thực sự đọc:

- Dùng đúng tên keychain của Claude Code 2.x: `Claude Code-credentials-<hash thư mục cấu hình>`, có fallback về tên cũ và về file.
- Ghi an toàn: cập nhật tại chỗ, **đọc lại để xác nhận** đã ghi đúng chỗ, **thử lại tối đa 3 lần** nếu Keychain tạm khóa.
- **Đồng bộ Keychain ↔ file**: nếu file credentials đang tồn tại, app ghi thêm bản sao ra file để tác vụ nền vẫn đọc được khi Keychain bị khóa — nhưng không tự tạo file mới nếu chưa có.

### 2. Các thao tác đều nhận đúng tài khoản trên macOS

Hiển thị/nhận diện tài khoản active, lưu profile, chuyển tài khoản, đồng bộ trạng thái (reconcile), làm mới token, đọc quota, priming và dữ liệu webhook — tất cả giờ đọc/ghi đúng đối tượng thay vì trượt vào một file không tồn tại.

### 3. Linux & Windows: không thay đổi

Trên Linux và Windows, app tiếp tục dùng file `~/.claude/.credentials.json` y như trước. Các profile đã lưu vẫn là file thường trên mọi hệ điều hành.

## Cập nhật

Mở **Cài đặt → Giới thiệu → Kiểm tra cập nhật**, hoặc tải bản mới nhất từ [GitHub Releases](https://github.com/ducdev2k1/agent-switch-tools/releases).
